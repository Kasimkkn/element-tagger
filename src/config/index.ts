export { DEFAULT_CONFIG } from './default-config';
export { loadConfig } from './config-loader';
export { ConfigValidator } from './config-validator';

// Re-export for convenience
export type { ElementTaggerOptions } from '../types/config';